# Everforest theme for tmux
# TPM plugin - using gruvbox as base (similar color palette)
set -g @plugin 'egel/tmux-gruvbox'

# Set gruvbox to dark mode (everforest is typically dark)
set -g @tmux-gruvbox 'dark'